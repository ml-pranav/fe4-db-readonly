# FE4 SQL Server Read-Only (Cursor plugin)

Placeholder for a Cursor plugin that will mirror the Oracle read-only plugin, but for **FE4 SQL Server**.

## Planned (not implemented yet)

- Pinned read-only SQL Server connection (no credentials from app repos)
- SELECT-only MCP tools
- Fail-closed hooks / rules / skills
- Windows `install.ps1` / `uninstall.ps1` similar to `../fe4-oracle-readonly/`

## Status

Stub only. Do not install from this folder yet.

When work starts, this directory will become a sibling project to [`fe4-oracle-readonly`](../fe4-oracle-readonly/), living in the same GitHub repository.
